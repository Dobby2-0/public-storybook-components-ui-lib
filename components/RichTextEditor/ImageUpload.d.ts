import { Editor } from '@tiptap/react';
interface ImageUploadButtonProps {
    editor: Editor;
    isDisabled?: boolean;
}
declare const ImageUploadButton: ({ editor, isDisabled }: ImageUploadButtonProps) => import("react/jsx-runtime").JSX.Element;
export default ImageUploadButton;
